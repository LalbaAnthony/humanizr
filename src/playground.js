console.log('Playground file');

const humanize = require('./index');

const text = "Il faut du courage pour affronter ses ennemis mais il en faut encore plus pour affronter ses amis";
console.log(humanize.typos(text));
